interface:
  display_name: "Reaper2MA Frontend"
  short_description: "Maintain the React/Vite static frontend"
  default_prompt: "Use $reaper2ma-frontend to update the React/Vite UI, local project flow, static build, or deployment behavior."
